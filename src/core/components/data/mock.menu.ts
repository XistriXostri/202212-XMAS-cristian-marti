import { MenuItem, MenuItems } from '../../types/menu.item';

export const mockMenu: MenuItem = { path: '/home', label: 'home' };

export const mockArrayMenu: MenuItems = [
    { path: '/home', label: 'Home' },
    { path: '/robots', label: 'Robots' },
];
